<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lyubishevtime.mapper.TimeEventTagOrderMapper">
    <insert id="upsert">
        INSERT INTO time_event_tag_order (user_id, tag_ids) VALUES (#{appUser.id}, #{tagIdsArr})
        ON CONFLICT (user_id) DO UPDATE
        SET tag_ids = #{tagIdsArr}, updated_at = now()
    </insert>
</mapper>